New, faster way.
in VM ssh terminal:

cd physapp
git pull origin main
npm install
npm run build
pm2 restart physapp





_____________________




1. Run locally:
npm run build

2. 
Delete previous folders in SSH terminal:
cd physapp
rm -rf .next public

Upload only these in Filezilla:

.next/ (The build output)

public/ (Images and static assets)

package.json

package-lock.json (or yarn.lock)

next.config.js

3. in VM ssh:

npm install --production
pm2 restart physapp